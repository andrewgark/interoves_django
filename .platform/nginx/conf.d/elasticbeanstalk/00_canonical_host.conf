# Public TLS is terminated by Cloudflare and the ALB has an HTTP-only listener.
# Redirect only the non-canonical hostname here; redirecting all origin HTTP
# traffic would loop requests that were HTTPS at the browser.
if ($host = www.interoves.com) {
    return 308 https://interoves.com$request_uri;
}
