# Timing log (format in /etc/nginx/conf.d/00_interoves_timing_format.conf via ebextension).
# Separate file so default combined access.log stays unchanged.
access_log /var/log/nginx/access_timing.log interoves_timing;
