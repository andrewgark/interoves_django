# Cheap steady-state reconciliation; only invalid/missing releases are rebuilt.
files:
  "/opt/interoves/daily_result_projection_cron.sh":
    mode: "000755"
    owner: root
    group: root
    content: |
      #!/bin/bash
      set -euo pipefail
      APP_DIR=/var/app/current
      LOG=/var/log/daily_result_projection_cron.log
      LOCK=/var/lock/daily_result_projection_cron.lock
      exec 9>"$LOCK"
      if ! flock -n 9; then
        exit 0
      fi
      {
        echo "---- $(date -Is) reconcile_daily_result_projections ----"
        python3 - "$APP_DIR" <<'PY'
      import glob
      import os
      import django
      import socket
      import subprocess
      import sys

      app_dir = sys.argv[1]
      pid = subprocess.check_output(['pgrep', '-of', 'daphne'], text=True).strip()
      env = os.environ.copy()
      with open('/proc/{}/environ'.format(pid), 'rb') as source:
          for item in source.read().split(b'\0'):
              if b'=' in item:
                  key, value = item.split(b'=', 1)
                  env[key.decode()] = value.decode()
      python = sorted(glob.glob('/var/app/venv/*/bin/python'))[-1]
      os.environ.update(env)
      os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'interoves_django.settings')
      sys.path.insert(0, app_dir)
      django.setup()
      from games.queue_heartbeat import heartbeat
      with heartbeat('daily_result_projection_reconcile', worker='cron:{}'.format(socket.gethostname())):
          subprocess.run(
              [python, 'manage.py', 'reconcile_daily_result_projections',
               '--apply', '--limit', '5'],
              cwd=app_dir, env=env, check=True,
          )
      PY
      } >>"$LOG" 2>&1

  "/etc/cron.d/interoves-daily-result-projection":
    mode: "000644"
    owner: root
    group: root
    content: |
      SHELL=/bin/bash
      PATH=/sbin:/bin:/usr/sbin:/usr/bin
      * * * * * root /opt/interoves/daily_result_projection_cron.sh

commands:
  01_daily_result_projection_cron_log:
    command: "touch /var/log/daily_result_projection_cron.log && chmod 644 /var/log/daily_result_projection_cron.log"
